const {
    loadTemplate,
    extractVariables,
    renderTemplate,
    processHTMLWithCheerio,
} = require('./index');

module.exports = {
    loadTemplate,
    extractVariables,
    renderTemplate,
    processHTMLWithCheerio,
};
